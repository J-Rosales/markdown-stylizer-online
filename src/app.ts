import "./style.css";

const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.innerHTML = `
    <h1>Markdown Stylizer Online</h1>
    <p>Milestone 0: build and run baseline</p>
  `;
}
